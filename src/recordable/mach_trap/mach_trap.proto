syntax = "proto3";

package mrr.recordable.mach_trap;

message MachTrap {
  uint32 trap_number = 1;
  message Timebase { bytes data = 1; }
  oneof data {
    uint64 return_only = 2;
    Timebase timebase = 3;
  }
}
